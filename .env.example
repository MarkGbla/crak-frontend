# Existing CRAK FastAPI service. Keep server-side when possible.
CRAK_API_URL=http://localhost:8000

# Optional browser-visible API URL for client-side polling.
NEXT_PUBLIC_CRAK_API_URL=http://localhost:8000

# Add Clerk keys when authentication is wired for the target environment.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
